# UI Rules

- UI components should contain display logic only, not business rules.
- Handle loading, empty, error, and normal states where applicable.
- Prefer reusable components and shared design tokens when available.
- The app should show a usable initial screen with default configuration.
