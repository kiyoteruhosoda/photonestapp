# Git Rules

- Use one commit per logical change.
- Commit message format: `<type>(<scope>): <summary>`.
- Common types: `feat`, `fix`, `refactor`, `test`, `docs`, `chore`.
- Before commit, review the diff and run relevant checks.
