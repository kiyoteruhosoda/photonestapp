# Progress — 進行中タスク

**このファイルには進行中・未着手・要判断のタスクだけを置く。**

- 完了したら本ファイルから消す。要約は `CHANGELOG.md` へ、経緯が要るものは
  `history/` へ移す。**完了項目の運用メモを本ファイルに残さない**
  （手順は `OPERATIONS.md` / `DEEP_LINKS.md`、設計の取り決めは
  `ARCHITECTURE.md`、判断の理由は `adr/`、過去の経緯は `CHANGELOG.md`）。
- 設計判断そのもの（何をどう決めたか・なぜ他案を採らなかったか）は ADR に
  書く。Progress からは「その判断が要る」ことだけを指す。
- **機能を増やす項目は、`CLAUDE.md`「プロダクトの範囲」を満たすことを先に
  示す。** 満たせないものはここに載せず、見送りとして ADR へ書く。
- 状態: ⬜未着手 / 🚧進行中 / 🟡要判断
- 影響度・重要度・難易度・工数: 大 / 中 / 小
- バックログは「優先」列の昇順（1 が最優先）。

## バックログ

A6・A12・A7 は ADR-0010 の適用でいったん「入れるかどうか」から問い直したが、
**入れると判断した**（ADR-0010「帰結」に記録）。全項目が着手対象。

| 優先 | # | 概要 | 状態 | 影響度 | 重要度 | 難易度 | 工数 |
|---|---|---|---|---|---|---|---|
| 1 | A8 | release 署名のフィンガープリントが未記入で App Link が検証されない | 🟡要判断 | 大 | 大 | 小 | 小 |
| 2 | A10 | 自動バックアップの対象を選べず、端末の写真・動画すべてを送る | ⬜未着手 | 大 | 中 | 中 | 中 |
| 3 | A12 | 新しいタグをアプリから作れない | ⬜未着手 | 小 | 中 | 小 | 小 |
| 4 | A6 | アルバムの作成・編集がアプリから触れない | ⬜未着手 | 中 | 中 | 中 | 中 |
| 5 | A7 | アカウント設定がアプリから触れない | ⬜未着手 | 小 | 小 | 中 | 大 |

A12 を A6 より先に置いた。画面が増えず（タグエディタに入力欄を 1 つ足すだけ）、
「見ている写真をその場で整理する」というタグ編集を入れたときの狙いに直接乗る
ため。A6 はフォームが 2 画面増えるぶん重い。

## 詳細

1. **A8 — release 署名のフィンガープリント。** `docs/deep_links/assetlinks.json`
   は `package_name` と debug 署名の指紋まで入っているが、release 分は
   `REPLACE:WITH:YOUR:RELEASE:KEYSTORE:SHA256:FINGERPRINT` のまま。埋まるまで
   release ビルドの App Link は検証されず、リンクはアプリではなくブラウザで開く。
   **鍵を持つ人にしか進められない**ので要判断のまま置く。手順は
   `docs/DEEP_LINKS.md`「`assetlinks.json` の配置」。Play App Signing を使う
   場合は、アップロード鍵ではなく Play Console の「アプリの署名鍵の証明書」の
   指紋を使う。

2. **A10 — バックアップ対象の選択。** `SyncNewPhotosUseCase` は端末のライブラリ
   全体を対象にする。`PhotoManagerPhotoLibraryGateway.photosTakenAfter` は
   `onlyAll: true` かつ `RequestType.common` なので、**静止画だけでなく動画も
   すべて含む**。スクリーンショット・メッセージアプリが受信した画像・保存した
   ミームに加え、画面録画や受信動画まで NAS へ上がるため、ライブラリが実際の
   写真で埋もれ、回線とストレージも食う。端末側のアルバム
   （`AssetPathEntity`）単位で対象を選べるようにする——**受け入れ確認は
   写真と動画の両方で行う**（片方だけ見ていると、選ばなかったアルバムの動画が
   上がり続ける取りこぼしに気づけない）。合わせて「充電中のみ」の条件も
   検討する（現状は Wi-Fi 限定の切り替えだけ）。

3. **A12 — タグの新規作成。** 今できるのは、既にライブラリにあるタグの
   付け外しだけ（`POST /api/tags` を呼んでいない）。新しいタグを起こすには
   Web の管理画面へ行く必要がある。**必要な scope は `media:tag-manage` だけ**
   ——サーバーの `POST /api/tags` は付け外しと同じコードを要求し、`tag:create`
   という権限コードは存在しない。したがってタグエディタが開けている時点で
   作成もできる（A9 で入れた `MediaPermission.tagMedia` がそのまま使える）。
   **画面は増やさない**——タグエディタの検索欄で一致するタグが無いときに
   「『〇〇』を作る」を出す形にする。作成後はそのまま選択済みにする。

4. **A6 — アルバムの作成と編集。** 今は一覧と詳細の閲覧だけ
   （`AlbumRepository.findAll` / `findById`）。作成は `POST /api/albums`、
   更新は `PUT /api/albums/{id}`、いずれも `album:create` / `album:edit` scope が
   要る。**収録メディアは差分ではなく `mediaIds` の全置換で送る**
   （`POST /albums/{id}/items` のような追加専用のエンドポイントは無い）ため、
   「この写真をアルバムへ入れる」を作るなら、対象アルバムの現在の収録を
   全ページ読んでから 1 件足した配列を PUT することになる。大きいアルバムほど
   往復が増えるので、**この往復を実測してから詳細ビューアの導線を決める**
   （代替は追加専用エンドポイントをサーバーへ足す案。そちらへ倒すなら
   `photonest` 側に項目を起こす）。境界はタグと同じ形を使える（閲覧の
   repository とは分ける・結果は読み込み済みの状態へ書き戻す・サーバーが
   確定した値を採る）。作成フォームと編集フォームの 2 画面が増える。

5. **A7 — アカウント設定。** プロフィール／パスワード変更・TOTP・パスキー。
   写真を見ている最中に必要になるものではないので最後に置く。**パスキー
   （`/api/auth/passkeys`）は切り離して判断する**——端末側の WebAuthn 対応が
   要り、他の 3 つとは重さが違う。設定タブに 1 画面足す形にとどめ、Web の
   管理画面と同じ密度の画面をアプリへ持ち込まない。
